1. 저장소 생성
git init
: 해당 폴더를 깃 저장소로 생성할 준비

git remote add origin https://github.com/xo0ol/폴더명.git
: https://github.com/아이디/저장소이름.git 저장소 연결
33


2. 저장소 확인

ls -alt




3. 파일 업로드

git add .

git commit -m 메세지입력

git push origin master




4. 연결된 원격저장소  확인하기

git remote -v



5. 브랜치 만들기

git branch 브랜치명

git checkout 브랜치명

git push origin git_test_sub1



6. 브랜치 삭제하기

git branch -d 브랜치명

git push origin --delete 브랜치명

git branch -a
: 브랜치 확인하기